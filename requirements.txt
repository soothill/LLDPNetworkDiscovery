# LLDP Network Discovery - Python Dependencies
# Install with: pip install -r requirements.txt

# Required: SSH automation and network device management
netmiko>=4.0.0

# Optional: Network diagram generation (Python library)
# Note: Also requires Graphviz system package to be installed
# Debian/Ubuntu: sudo apt-get install graphviz
# RHEL/CentOS: sudo yum install graphviz
# macOS: brew install graphviz
graphviz>=0.20
